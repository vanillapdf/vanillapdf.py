sphinx>=7.4,<8
sphinx-rtd-theme>=3.0,<4
sphinx-copybutton>=0.5,<1
sphinxext-opengraph>=0.9,<1
# Used by conf.py to derive the version from git tags when the package is not
# installed (e.g. on a fresh Read the Docs checkout).
setuptools-scm>=9
